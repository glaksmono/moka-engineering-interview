<h1>Editing item</h1>

<%= form_for @item, url: item_path(@item), html: {class: "horizontal_form"} do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>


  <%= f.label :price %>
  <%= f.text_field :price %>

  <%= f.submit "Update" %>
<% end %>
